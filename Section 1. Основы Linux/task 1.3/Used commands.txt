sudo nano /usr/local/bin/uptime_logger.sh
sudo chmod +x /usr/local/bin/uptime_logger.sh
sudo nano /etc/systemd/system/uptime-logger.service
sudo systemctl daemon-reload
sudo systemctl enable uptime-logger.service
sudo systemctl start uptime-logger.service
sudo systemctl status uptime-logger.service

stress -с 6 -t 120s

sudo crontab -e (nano)
		"*/10 * * * * /bin/systemctl status uptime-logger.service > /var/log/uptime-logger-status.log 2>&1"
sudo crontab -l

ping google.com &
jobs
fg
kill


